import { makeStyles } from '@mui/styles'

const useStyles = makeStyles({
  outerWrapper: { padding: "5em 2em" },
  label: { fontSize: "16px" },
  innerWrapper: { backgroundColor: "whitesmoke", borderRadius: "12px", padding: "2em 0" },
  inlineElement: { display: "inline" },
})

export default useStyles;